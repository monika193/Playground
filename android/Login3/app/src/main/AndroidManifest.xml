<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanjoth.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Login"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Forgotpassword" />
        <activity
            android:name=".MyRequest"
            android:label="@string/title_activity_my_request"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".recycleview"
            android:label="@string/title_activity_recycleview"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".jsonfile" />
        <activity
            android:name=".Requisition1"
            android:label="@string/title_activity_requisition1"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>